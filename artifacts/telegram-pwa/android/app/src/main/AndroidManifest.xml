<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          ── Custom FirebaseMessagingService ─────────────────────────────
          Overrides the one bundled with @capacitor/push-notifications so
          we can build rich MessagingStyle notifications with InboxStyle
          stacking (per-conversation thread + per-tray summary) and an
          inline RemoteInput "Répondre" action — same UX as WhatsApp /
          Telegram. The Capacitor plugin still owns FCM token registration
          via FirebaseMessaging.getToken() at JS-level register() time, so
          we don't need its service for that. Server now sends data-only
          payloads on Android (see lib/fcm.ts) so this service is the
          sole notification builder.
        -->
        <service
            android:name=".FcmMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Receiver for inline RemoteInput "Répondre" + "Lu" actions. -->
        <receiver
            android:name=".NotificationReplyReceiver"
            android:exported="false" />

        <!-- Default notification channel hint for the FCM SDK so any
             stray system-displayed notification (extremely rare with
             our data-only setup) lands on the right channel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="messages" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>

    <!-- ── Permissions ─────────────────────────────────────────────────── -->
    <!-- Réseau : nécessaire pour charger l'app et les API. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Micro : messages vocaux + appels audio/vidéo. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Caméra : photo / vidéo dans le chat + appels vidéo. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Stockage / médias : envoyer photos & vidéos depuis la galerie. -->
    <!-- Android 13+ utilise les permissions granulaires READ_MEDIA_*.
         Les versions plus anciennes utilisent READ_EXTERNAL_STORAGE. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Notifications push (Android 13+ exige une permission runtime). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration : retours haptiques + notifications d'appel. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Garde l'écran allumé pendant un appel. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service : permet à WebRTC de continuer à tourner
         quand l'utilisateur quitte l'app pendant un appel long.
         Sans ça Android suspend la WebView au bout de quelques minutes
         et l'appel se coupe. FOREGROUND_SERVICE_MICROPHONE +
         _CAMERA sont les sous-types exigés par Android 14+ pour les
         services qui captent l'audio/la caméra en arrière-plan. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <!-- Bluetooth : routage audio (kit mains-libres / écouteurs) en appel. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Installation d'APK : permet au téléchargement (DownloadManager) de
         déboucher sur le prompt d'installation natif quand l'utilisateur
         tape sur la notification "Mise à jour téléchargée". -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Indique que la caméra et le micro sont optionnels (pour rester
         compatible avec les tablettes ou Chromebooks sans hardware). -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
</manifest>
